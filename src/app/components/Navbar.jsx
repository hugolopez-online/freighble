const Navbar = () => {
    return (
        <nav className="navbar">
            <h1 className="fw-bold">
                <i className="bi bi-record-circle-fill fs-2"></i> Freighble
            </h1>
        </nav>
    );
};

export default Navbar;
